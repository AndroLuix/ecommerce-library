function ListBooks() {
    //  document.getElementById("button").innerHTML = "Aggiungi al gruppo";

    let book = document.getElementById('select-book').value
    let form = document.getElementById('form-massive')
    let inputTitle = document.createElement('input')
     inputTitle.setAttribute('type', 'text');
     inputTitle.setAttribute('name','title');
     input.value = option.getAttribute('data-title')
    form.appendChild(input);
    //inputWithAttr.value = 

    //alert(book);
}
